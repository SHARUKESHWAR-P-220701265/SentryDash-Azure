apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sentrydash-ingress
  namespace: sentrydash
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sentrydash-frontend
            port:
              number: 80
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: sentrydash-backend
            port:
              number: 3000
