apiVersion: v1
kind: Service
metadata:
  name: sentrydash-frontend
  namespace: sentrydash
spec:
  type: LoadBalancer
  selector:
    app: sentrydash-frontend
  ports:
    - port: 80
      targetPort: 80
